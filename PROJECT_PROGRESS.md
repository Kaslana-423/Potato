# Potato 项目进度

更新时间：2026-09-10

## 当前里程碑

项目当前里程碑是保持一条稳定、可维护的垂直切片：

`Title → Save Slot → Main Menu → Character Select → Wave Combat → Level/Crate Rewards → Shop → Next Wave → Result`

本文档不使用完成度百分比，也不把电子表格、生成代码或占位配置的数量当作可玩内容数量。

状态含义：

- **Implemented**：能通过正常流程使用，并已有 Play Mode 验证依据。
- **Partial**：代码、场景或资源已经存在，但当前整合仍需 Play Mode 验收或功能尚未完全接入。
- **Reference only**：只作为开发资料存在，没有完整运行时实现。
- **Out of scope**：不属于当前里程碑，除非用户明确提出，否则不开发。

## 状态总览

| 系统 | 状态 | 当前证据与边界 |
| --- | --- | --- |
| 标题、存档槽、主菜单、角色选择 | **Implemented** | MainMenu 场景已连接标题、三槽存档、主菜单、薄荷选择和页面返回流程 |
| 薄荷与木棍 | **Implemented** | 当前正式角色和初始武器；木棍通过正常新 Run 获取 |
| 玩家基础战斗 | **Implemented** | 移动、自动索敌、生命、受伤、死亡和已接入的基础属性可用于正常战斗 |
| 基础武器模板 | **Implemented** | 斩击、突刺、远程三类已有可工作的攻击模板 |
| 基础敌人与掉落 | **Implemented** | 已接入的敌人支持生成、追踪、接触伤害、击退、死亡、复用和材料/水果/箱子掉落 |
| 经验、升级和箱子奖励 | **Implemented** | 波后按队列处理待升级次数和待处理箱子，然后进入商店 |
| 商店核心 | **Implemented** | 已接入商品支持刷新、锁定、购买、负担状态、合成、背包和详情显示 |
| 新地图、边界和相机 | **Partial** | 场景引用和限制逻辑已接入；需完成新地图下的连续波次 Play Mode 回归 |
| 门动画与波末镜头展示 | **Partial** | 下门开启入场后上下门同步关闭；波末虚拟相机移向上门并播放开门，再返回玩家。控制器、预建场景对象和门素材已绑定，需验证新游戏、下一波、继续游戏和最终波分支 |
| 波次倒计时与刷怪修复 | **Partial** | 计时、刷怪计划及无规则时的收尾路径已恢复；需在新地图实测倒计时和持续刷怪 |
| 武器状态统一 | **Partial** | `WeaponBag` 是 `IPlayerWeaponLoadout` 的可写来源，场上武器监听变化后重建表现；购买、合成、保存和恢复需要一轮完整回归 |
| SampleScene 商店 UI | **Partial** | 商品卡、商品图和详情对象已预制并绑定；运行时缺少引用会明确停用商店，仍需检查所有卡片和字体布局 |
| 设置界面 | **Partial** | MainMenu 作为视觉基准同步到 SampleScene，主音量为 10 档；需检查两场景字体、布局和持久化 |
| TMP 字体缺字处理 | **Partial** | `bups 2` 与 `ShanHaiNiuNaiBoBoW-2 SDF` 已启用动态多图集，并依次接入可用的中文与通用回退字体；资源引用静态有效，仍需 Play Mode 检查实际字形和控制台警告 |
| 纸片跳动表现 | **Partial** | `PlayerVisuals` 已实现移动跳起、左右倾斜、落地压缩；只改变 `PlayerSkin`，需要 Play Mode 确认手感 |
| Run 保存与恢复 | **Partial** | 能保存角色、阶段、波次、生命、材料、经验、背包和商店状态；尚未证明能精确恢复波内计时、刷怪进度和全部瞬时对象 |
| 其他角色 | **Partial** | 斗士、游侠和幸运星只有配置或占位内容，保持锁定，不计入可玩角色 |
| 导入武器、道具和敌人资料 | **Reference only** | XLSX、生成定义和未接入敌人只用于参考；没有完整效果、资源与获取路径的条目不得进入运行时池 |
| 完整原作/DLC 内容、联网与平台功能 | **Out of scope** | 当前不进行全量复刻、多人、在线服务、成就、本地化或平台 SDK 开发 |

## 已实现的垂直切片基础

### 菜单与存档

- 主场景顺序为 MainMenu、SampleScene。
- 支持选择存档槽、新建 Run、继续已有 Run、删除存档和返回上一层。
- 正式存档、临时存档和备份文件位于 `Application.persistentDataPath`。
- 主音量与全屏使用 `PlayerPrefs` 保存。
- 没有有效 Run 时，`Continue` 显示为不可执行状态。

### 玩家与武器

- 玩家刚体冻结旋转，移动时始终保持竖直。
- `PlayerController` 根据玩家碰撞体范围限制位置，避免贴墙后中心点仍可越界。
- 薄荷的新 Run 由 `WeaponBag.EnsureStartingWeapon` 保证木棍在战斗开始前存在。
- `WeaponBag` 保存武器负载；`PlayerWeaponEquipment` 只根据负载创建场上攻击表现。
- 已接入武器支持背包上限、购买、同类同品质合成及保存数据。

### 波次与场景

- `EnemySpawner` 负责计时、刷怪计划、敌人生成、波次结束和波后顺序协调。
- 敌人使用生成池，回收时会停止移动并重置运行状态。
- 波次结束后处理存活敌人、掉落、玩家恢复、升级奖励、箱子奖励和商店。
- `ArenaGateController` 使用 SampleScene 中预先创建的 `TopDoorOpenBackground`、`TopDoorVisual`、`BottomDoorVisual` 和 `GateCameraFocus`，不在运行时创建门对象。
- 波次开始时下门先开启让玩家入场，随后上下门一起关闭并恢复战斗控制；波末暂时锁定玩家，虚拟相机移动到上门、播放开门效果，再返回玩家并允许离场。
- 相机与玩家使用新地图的边界引用；玩家初始化时会将保存位置限制回有效区域。

### 商店

- SampleScene 已包含商品卡、商品图、描述面板、刷新按钮、武器背包和道具背包对象。
- `ShopManager` 只使用完整绑定的场景商品卡；引用缺失时记录错误并停止运行，不创建替代商品 UI。
- 商品可按波次与幸运权重生成，支持刷新费用、免费刷新、锁定和购买限制。
- 商品说明和背包详情使用 TextMeshPro，当前字体最大值已调整以避免面板中文字过小。

### 设置与视觉

- MainMenu 的 SettingPanel 是当前设置界面的视觉来源。
- SampleScene 的暂停设置沿用相同纸张颜色、字体、标题、返回按钮和音量条结构。
- 音量 Slider 使用 `0..10` 整数值，并在保存时转换为 `0..1` 标准音量。
- `bups 2` 使用自身 OTF 动态补字，并回退到 `ShanHaiNiuNaiBoBoW-2 SDF`、SmileySans SDF 和 LiberationSans SDF；山海字体回退到 SmileySans SDF 和 LiberationSans SDF。两者均允许动态多图集扩展。
- 退出游戏确认框使用场景中已有文字，不在打开窗口时重新生成退出提示文本。
- `PlayerVisuals` 在移动时对 PlayerSkin 做跳动、拉伸和落地压缩；玩家根对象和碰撞体不缩放。

## 当前缺口

### 运行时内容

- 爆炸、燃烧、弹射、穿透、连锁闪电、炮塔等效果家族没有形成完整且经过验收的实现。
- 部分属性能保存或显示，但没有接入实际战斗计算。
- 大量导入定义没有有效 Prefab、独立行为、完整资源或经过验证的获取路径。
- 斗士、游侠和幸运星没有达到角色验收门槛。

### 集成与回归

- 新地图、门动画、波末相机展示、波次、奖励和商店刚完成整合，需要从 MainMenu 开始进行连续流程测试，并检查门素材对齐、开关时序和镜头返回手感。
- 武器负载统一后，需要覆盖购买、合成、移除、保存、重载和下一波切换。
- 设置界面需要在常用分辨率下检查锚点、字体大小和 10 档点击区域；中英文、数字和常用符号还需确认没有缺字或 TMP 警告。
- 当前没有覆盖完整游玩流程的自动化测试。

### 保存精度

- 存档包含当前 Run 阶段和主要持久状态。
- 波内剩余时间、当前刷怪计划进度、场上敌人和掉落物不应描述为精确恢复，除非以后补齐并单独验证。
- 任何存档结构调整都要保留稳定内容 ID，并考虑旧槽位迁移。

## Play Mode 验收清单

以下步骤是当前改动完成验收所必需的人工检查：

1. 从 MainMenu 选择空槽位，选择薄荷并创建新 Run。
2. 确认木棍在第一波开始前已经出现在玩家手上并能攻击。
3. 确认下门先开启让玩家进入，随后上下门一起关闭，再出现倒计时并持续刷怪。
4. 沿四面墙移动，确认玩家不会旋转、被挤出地图或越过相机可见边界。
5. 结束一波，确认玩家暂时无法移动，虚拟相机移向上门并播放开门，然后平滑返回玩家；从上门离场后依次进入升级、箱子和商店。
6. 确认商店四张商品卡、图标、描述、价格、刷新、锁定和购买均可见且可执行。
7. 购买与合成武器，确认背包和玩家手上的武器数量、品质与攻击行为一致。
8. 开始下一波，确认再次从下门入场，武器不会延迟到波末才出现。
9. 分别打开 MainMenu 和 SampleScene 的设置界面，确认样式一致，音量能逐格调整并在重启后保留；检查中文、英文、数字和常用符号，确认没有缺失字形或 TMP 警告。
10. 移动与停止角色，确认纸片跳动自然，且视觉缩放不影响碰撞和武器位置。
11. 分别在战斗、波后奖励和商店阶段保存并继续，核对阶段、生命、材料、经验、武器和商品状态。
12. 验证死亡结算；完整 20 波流程仍需单独进行平衡与胜利回归。

## 当前验证记录

- 2026-09-09：`Assembly-CSharp-Editor.csproj` 构建成功，0 警告、0 错误。
- 2026-09-09：SampleScene YAML 本地对象 ID 无重复，场景内本地 `fileID` 无缺失引用。
- 2026-09-10：门动画与波末相机展示相关代码构建成功，0 警告、0 错误；SampleScene 共 1472 个 YAML 文档，本地对象 ID 无重复且本地 `fileID` 无缺失引用。
- 2026-09-10：`bups 2` 的源字体引用及两套 TMP 字体的回退字体 GUID 均可解析；实际动态补字效果仍需 Play Mode 验证。
- MainMenu 和 SampleScene 均已加入 Build Settings。
- 当前静态检查不能代替上述 Play Mode 验收；尚未完成的步骤保持 **Partial**。

## 后续优先级

1. 完成新地图下从 MainMenu 到第二波商店结束的回归测试，重点检查门动画、波末镜头和字体缺字。
2. 修复回归中发现的门、镜头、波次、商店和初始武器问题。
3. 验证战斗、波后和商店三个保存阶段，明确可恢复范围。
4. 过滤未通过验收的导入条目，确保不会进入商店、奖励或敌人池。
5. 只为当前可玩内容补齐必要的命中、音效和界面反馈。
6. 完成 20 波流程测试和现有内容平衡。
