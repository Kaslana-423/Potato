# Potato 项目进度

更新时间：2026-09-11

## 当前里程碑

当前里程碑是保持一条稳定、可维护的垂直切片：

`Title → Save Slot → Main Menu → Character/Weapon Select → Wave Combat → Level/Crate Rewards → Shop → Next Wave → Result`

本文档不使用完成度百分比，也不把电子表格、生成代码或占位配置的数量当作可玩内容数量。

- **Implemented**：正常流程可用，并已有 Play Mode 验证依据。
- **Partial**：代码、场景或资源已经存在，但仍需 Play Mode 验收或整合尚未完全确认。
- **Reference only**：仅作为开发资料，没有完整运行时实现。
- **Out of scope**：不属于当前里程碑，除非用户明确提出，否则不开发。

## 状态总览

| 系统 | 状态 | 当前证据与边界 |
| --- | --- | --- |
| 标题、存档槽、主菜单、角色选择 | **Implemented** | MainMenu 已连接标题、三槽存档、主菜单、薄荷选择与页面返回流程 |
| 玩家基础战斗 | **Implemented** | 移动、自动索敌、生命、受伤、死亡和已接入基础属性可用于正常战斗 |
| 基础武器模板 | **Implemented** | 斩击、突刺、远程三类已有可工作的攻击模板 |
| 基础敌人与掉落 | **Implemented** | 已接入敌人支持生成、追踪、接触伤害、击退、死亡、复用和材料/水果/箱子掉落 |
| 新地图、边界和相机 | **Partial** | 新场景引用与位置限制已连接，需要连续波次 Play Mode 回归 |
| 门动画与波末镜头 | **Partial** | 下门入场、双门关闭、上门开启和镜头往返已接入，需要验证新 Run、下一波、继续游戏与最终波 |
| 波次倒计时与刷怪 | **Partial** | 计时、刷怪计划和无规则时的收尾路径已恢复，需要在新地图验证持续刷怪和倒计时 |
| 初始武器选择 | **Partial** | 新 Run 可在虚灵斧与燧石之间选择，选择结果由 `GameSessionState` 传给 `WeaponBag`；需要验证两种武器首波立即出现并攻击 |
| 武器状态、合成与回收 | **Partial** | `WeaponBag` 是 `IPlayerWeaponLoadout` 的权威来源，手持表现监听负载；已有武器可同类同品质合成并回收，需要覆盖购买、保存、重载和下一波 |
| 商店交互 | **Partial** | 商品卡向下拖动购买，图钉负责锁定/解锁，场景内四张商品卡和详情面板已绑定；仍需检查输入、动画和完整购买流程 |
| 升级与箱子奖励 | **Partial** | 自动升级已移除；升级和箱子面板已预置在 `SampleScene`，字体统一为 `ShanHaiNiuNaiBoBoW-2 SDF`，升级属性栏采用商店样式；需要 Play Mode 验收奖励队列 |
| 胜利与失败结算 | **Partial** | `GameSettlementCanvas`、文字、统计和按钮已预置在 `SampleScene`；控制器不再运行时创建 UI，需要分别验证死亡和第 20 波胜利 |
| 次要属性清理 | **Partial** | 已删除 Materials Healing、Explosion Damage/Size、Structure Attack Speed/Range、Burning Speed/Spread；商店、升级和属性 Prefab 均同步为 15 项，旧枚举编号保留空位；需要检查实际 UI 排版与旧存档 |
| 设置界面 | **Partial** | MainMenu 为视觉基准，SampleScene 使用同类纸张布局；音量为 10 格竖向步进，需要验证两场景的一致性和持久化 |
| TMP 字体 | **Partial** | `bups 2` 与 `ShanHaiNiuNaiBoBoW-2 SDF` 已配置动态多图集及回退，需要 Play Mode 检查实际字形和 TMP 警告 |
| 纸片移动表现 | **Partial** | `PlayerVisuals` 对 `PlayerSkin` 应用跳起、倾斜和落地压缩，需要 Play Mode 调整手感 |
| Run 保存与恢复 | **Partial** | 可保存角色、初始武器、阶段、波次、生命、材料、经验、背包和商店状态；不能宣称精确恢复波内计时、刷怪进度和全部瞬时对象 |
| 其他角色 | **Partial** | 斗士、游侠和幸运星只有配置或占位内容，保持锁定，不计入可玩角色 |
| 导入武器、道具和敌人资料 | **Reference only** | XLSX、生成定义和未接入敌人仅用于参考；没有完整行为、资源与获取路径的条目不得进入运行时池 |
| 全量原作/DLC、联网与平台功能 | **Out of scope** | 当前不进行全量复刻、多人、在线服务、成就、本地化或平台 SDK 开发 |

## 最近完成的静态整合

### 场景内 UI

- `LevelUpCanvas`、`LootCrateRewardCanvas` 和 `GameSettlementCanvas` 已存在于 `SampleScene`。
- 三类窗口默认隐藏，由控制器更新已有文字、图像、按钮状态并切换显隐。
- `GameRunSettlementController.GetOrCreate()` 不再创建控制器或结算 UI；场景缺失时明确报错。
- 奖励与结算界面使用 `Canvas Scaler`、锚点布局和纸张色彩。
- 编辑器装配工具可重新构建奖励与结算面板，但不会在游戏运行时执行。

### 商店与武器

- 商品卡下拉超过阈值后发起购买并播放纸张落下动画。
- 图钉点击后插入或拔出，锁定商品在刷新时保留。
- 背包武器可通过详情面板合成或回收；操作后由统一负载事件刷新手持表现。
- 新 Run 的初始武器 ID 在虚灵斧和燧石中选择，背包只在为空时补入所选武器。
- 自动升级路径已移除，升级奖励必须由玩家选择。

### 属性

- 次要属性展示从 22 项减少到 15 项。
- 删除的 7 项属性不再存在于 `PlayerStats` 的字段、读写、显示和字符串解析中。
- 材料拾取不再执行 Materials Healing。
- 商店目录会过滤仍引用这些已删除属性的导入条目。
- 保留剩余 `PlayerStatId` 的原数值，旧存档不会因枚举整体前移而映射到错误属性；旧存档中的已删除 ID 会被忽略。

## 当前缺口

### 运行时内容

- 爆炸、燃烧、弹射、穿透、连锁闪电、炮塔等效果家族没有完整且经过验收的实现。
- 大量导入定义没有有效 Prefab、独立行为、完整资源或经过验证的获取路径。
- 斗士、游侠和幸运星没有达到角色验收门槛。

### 集成与回归

- 新地图、门动画、波末镜头、波次、奖励、商店和结算刚完成整合，需要从 MainMenu 开始跑完整流程。
- 武器负载需要覆盖选择、购买、合成、回收、保存、重载与下一波切换。
- 新预置 UI 需要在常用分辨率下检查锚点、字号、滚动范围和按钮命中区域。
- 当前没有覆盖完整游戏流程的自动化测试。

## Play Mode 验收清单

1. 从 MainMenu 选择空槽位和薄荷，分别选择虚灵斧、燧石建立新 Run。
2. 确认所选武器在第一波开始前已经显示并能攻击，不会等到波末才刷新。
3. 确认下门开门入场、上下门关闭、倒计时出现并持续刷怪。
4. 沿四面墙移动，确认玩家保持竖直、不会被挤出地图或越过相机可见边界。
5. 结束一波，确认镜头展示上门开启并返回玩家，然后依次进入升级、箱子和商店。
6. 确认升级与箱子窗口使用场景对象，字体无缺字，升级属性栏布局与商店一致。
7. 检查次要属性页只有 15 项，已删除的 7 项不会显示；拾取材料不再治疗。
8. 在商店向下拖动卡牌完成购买；点击图钉后刷新，确认锁定商品保留且图钉姿态正确。
9. 购买同类武器后完成合成，再回收武器，确认背包、手持表现、材料和保存数据一致。
10. 开始下一波，确认再次从下门入场且武器持续存在。
11. 分别在 MainMenu 和 SampleScene 检查设置界面、10 格音量、字体和持久化。
12. 让玩家死亡，确认场景内结算窗口显示“本局结束”，统计、重新开始和返回主菜单可用。
13. 完成第 20 波，确认同一结算窗口显示“胜利”，统计和两个按钮可用。
14. 分别在战斗、波后奖励和商店阶段保存并继续，核对阶段、生命、材料、经验、武器和商品状态。

## 当前验证记录

- 2026-09-11：`Assembly-CSharp-Editor.csproj` 构建成功，0 警告、0 错误。
- 2026-09-11：`SampleScene` 中商店和升级属性面板的次要属性均为 15 项，`PlayerStatsPanel.prefab` 同步为 15 项。
- 2026-09-11：已删除属性在运行代码、场景和 Prefab 中无残留引用；商店过滤令牌除外。
- 2026-09-11：结算控制器与编辑器装配代码编译成功；`GameSettlementCanvas` 的 45 个 Unity YAML 文档引用完整，无重复或缺失的结算本地引用。
- MainMenu 与 SampleScene 已加入 Build Settings。
- 静态检查不能替代上述 Play Mode 验收，近期改动继续保持 **Partial**。

## 后续优先级

1. 完成从 MainMenu 到第二波商店结束的连续回归，重点检查武器选择、门、镜头、奖励和商店交互。
2. 分别验证死亡结算和第 20 波胜利结算。
3. 验证战斗、波后与商店阶段的保存恢复边界。
4. 过滤所有未通过验收的导入条目，确保不会进入商店、奖励或敌人池。
